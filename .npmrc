# Override Lilly's global registry for this project.
# All dependencies (react, vite, etc.) are public npm packages
# and must resolve from the public registry in CI.
registry=https://registry.npmjs.org/
